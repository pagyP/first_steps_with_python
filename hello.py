#print("hello there!")
print("What is your name?")
name = input()
print("Hello, " + name)

#Keywords are commands built into the programming language and they have a special meaning.  They can't
#be used as variable names

#Operators
#An operator is a character that instructs the Python complier to perform some special operation
#Opening and closing parenthese () are function invocation operators when places to the right of the function name
#= is the assignment operator.  It is used to assign a value to a variable
# The = sign is the string concatenation operator.  Also the addition operator
# Is the comment operator
